country_event = {
	id = election
	title = election.1.t
	desc = election.1.d
	picture = GFX_report_event_election_vote
	
	is_triggered_only = yes
	immediate = {

        MLI = {
            if = {
                limit = { is_in_faction = no }
                create_faction = "Anti_Player_Alliance"
            }
        }

        NGR = {
            if = {
                limit = { is_in_faction = no }
                join_faction = GER
            }
        }

        CHA = {
            if = {
                limit = { is_in_faction = no }
                join_faction = GER
            }
        }
    }
	option = {
		name = election.1.a
	}
}